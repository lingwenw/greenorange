<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wpp.greenorange.dao.GoodsFavouriteDao">

    <resultMap type="com.wpp.greenorange.domain.GoodsFavourite" id="goodsFavouriteMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="skuId" column="sku_id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="statusId" column="status_id" jdbcType="INTEGER"/>
        <result property="deleted" column="deleted" jdbcType="BOOLEAN"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="findById" resultMap="goodsFavouriteMap">
        select
          id, sku_id, user_id, status_id, deleted, create_time, update_time
        from greenorange.tb_goods_favourite
        where id = #{id}
    </select>


    <!--通过实体作为筛选条件查询-->
    <select id="findAllByCondition" resultMap="goodsFavouriteMap">
        select
        id, sku_id, user_id, status_id, deleted, create_time, update_time
        from greenorange.tb_goods_favourite
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="skuId != null">
                and sku_id = #{skuId}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="statusId != null">
                and status_id = #{statusId}
            </if>
            <if test="deleted != null">
                and deleted = #{deleted}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into greenorange.tb_goods_favourite(sku_id, user_id, status_id, deleted, create_time, update_time)
        values (#{skuId}, #{userId}, #{statusId}, #{deleted}, #{createTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update greenorange.tb_goods_favourite
        <set>
            <if test="skuId != null">
                sku_id = #{skuId},
            </if>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="statusId != null">
                status_id = #{statusId},
            </if>
            <if test="deleted != null">
                deleted = #{deleted},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from greenorange.tb_goods_favourite where id = #{id}
    </delete>

</mapper>