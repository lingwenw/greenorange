<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wpp.greenorange.dao.GoodsSkuDao">

    <resultMap type="com.wpp.greenorange.domain.GoodsSku" id="goodsSkuMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="goodsId" column="goods_id" jdbcType="INTEGER"/>
        <result property="colorId" column="color_id" jdbcType="INTEGER"/>
        <result property="color" column="color" jdbcType="VARCHAR"/>
        <result property="skuEdition" column="sku_edition" jdbcType="VARCHAR"/>
        <result property="price" column="price" jdbcType="NUMERIC"/>
        <result property="stock" column="stock" jdbcType="INTEGER"/>
        <result property="deleted" column="deleted" jdbcType="BOOLEAN"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="findById" resultMap="goodsSkuMap">
        select
          id, goods_id, color_id, color, sku_edition, price, stock, deleted, create_time, update_time
        from greenorange.tb_goods_sku
        where id = #{id}
    </select>


    <!--通过实体作为筛选条件查询-->
    <select id="findAllByCondition" resultMap="goodsSkuMap">
        select
        id, goods_id, color_id, color, sku_edition, price, stock, deleted, create_time, update_time
        from greenorange.tb_goods_sku
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="goodsId != null">
                and goods_id = #{goodsId}
            </if>
            <if test="colorId != null">
                and color_id = #{colorId}
            </if>
            <if test="color != null and color != ''">
                and color = #{color}
            </if>
            <if test="skuEdition != null and skuEdition != ''">
                and sku_edition = #{skuEdition}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="stock != null">
                and stock = #{stock}
            </if>
            <if test="deleted != null">
                and deleted = #{deleted}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into greenorange.tb_goods_sku(goods_id, color_id, color, sku_edition, price, stock, deleted, create_time, update_time)
        values (#{goodsId}, #{colorId}, #{color}, #{skuEdition}, #{price}, #{stock}, #{deleted}, #{createTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update greenorange.tb_goods_sku
        <set>
            <if test="goodsId != null">
                goods_id = #{goodsId},
            </if>
            <if test="colorId != null">
                color_id = #{colorId},
            </if>
            <if test="color != null and color != ''">
                color = #{color},
            </if>
            <if test="skuEdition != null and skuEdition != ''">
                sku_edition = #{skuEdition},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="stock != null">
                stock = #{stock},
            </if>
            <if test="deleted != null">
                deleted = #{deleted},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from greenorange.tb_goods_sku where id = #{id}
    </delete>

</mapper>